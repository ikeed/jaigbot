[pytest]
addopts = -ra -q --cov=app --cov-report=term-missing --cov-config=.coveragerc
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
markers =
    live_llm: requires real GCP credentials and Vertex AI access (slow, non-deterministic)
